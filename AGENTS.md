# AGENTS.md

학습 워크스페이스 저장소다. 레슨과 참조 문서를 HTML로 쓰고 GitHub Pages로 배포한다.
이 파일은 **그 문서들을 어떤 문체로 쓰는지**를 정한다.

## 레슨은 에세이가 아니라 설명서다

읽는 사람은 그 주제를 모른다. 그래도 노력하면 읽을 수 있어야 한다.
그러니 긴장을 만들지 말고 **순서대로 알려 준다**.

극적인 강조, 수사의문문, 반전 구조는 정보를 담지 않는다.
"여기가 오늘의 전부입니다" 같은 문장은 지운다.

기준은 [ASD-STE100](https://www.asd-ste100.org/) 이다. 영어 표준이라 그대로 옮길 수 없다.
아래 열한 개만 한국어로 옮겨 쓴다.

## 규칙

`검사` 표시가 있는 것은 `tools/style-check.py` 가 기계로 잡는다.
숫자의 출처는 이 파일이다. 바꾸려면 여기를 먼저 고치고 스크립트를 맞춘다.

### 문장

| | 규칙 | |
|---|---|---|
| 1 | 한 문장에 한 가지만 말한다 | |
| 2 | 문장은 20어절까지 쓴다 | 검사 |
| 3 | 한 문단에 한 주제를 담고 6문장까지 쓴다 | 검사 |
| 4 | 줄표(—)와 괄호는 문장당 한 개까지 쓴다 | 검사 |

### 말

| | 규칙 | |
|---|---|---|
| 5 | 새 용어는 처음 나올 때 한 문장으로 정의하고 용어집에 링크한다 | |
| 6 | 한 용어는 한 뜻으로만 쓴다. 같은 것을 다른 말로 바꿔 부르지 않는다 | |
| 7 | 명사를 세 개 넘게 이어 붙이지 않는다 | 검사 |
| 8 | 명사화 대신 동사를 쓴다. "검증의 가능성"이 아니라 "검증할 수 있다" | 검사 |

용어집은 `<topic>/reference/0000-glossary.html` 이다. 그 워크스페이스 용어의 단일 출처다.
레슨에서 용어를 정의하면 용어집에도 올린다. 정의가 두 곳에서 갈라지면 용어집이 맞다.

### 주장

| | 규칙 | |
|---|---|---|
| 9 | 주장에는 출처 링크를 붙인다 | |
| 10 | 인용한 주장과 내가 이은 추론을 구분해서 표시한다 | |

10번은 신뢰의 문제다. 근거 두 개를 이어 새 결론을 만들었다면 그렇게 적는다.
`ai/lessons/0002-retrieval-loop.html` 의 "여기서부터는 추론입니다" 블록이 예다.

### 퀴즈

| | 규칙 | |
|---|---|---|
| 11 | 보기는 글자 수를 공백까지 정확히 맞춘다 | |

길이 차이가 정답을 흘린다. 쓴 뒤에 세어서 확인한다.

## 문서를 고친 뒤에 할 일

두 명령이 통과해야 끝난 것이다.

```sh
python3 tools/style-check.py <고친 파일들>   # 위반 0 이어야 한다
python3 tools/build-index.py                    # 목록 페이지를 다시 만든다
```

그리고 세 가지를 눈으로 확인한다.

- 새로 쓴 용어가 전부 용어집에 있는가
- 퀴즈 보기의 글자 수가 같은가
- 마크다운 파일로 거는 링크가 있는가.
  `tools/build-index.py` 는 `.md` 와 `learning-records/` 를 배포에서 걸러낸다.
  상대경로로 걸면 배포된 사이트에서 404 가 난다. 저장소 blob URL 로 건다.

## 적용 범위

**독자가 읽는 문서에 적용한다.** `lessons/` 와 `reference/` 가 그것이다.
`MISSION.md` 도 레슨에서 링크하므로 포함한다.

`NOTES.md` 와 `RESOURCES.md` 와 `learning-records/` 는 **작업 기록**이다.
여기 규칙을 강제하지 않는다. 검사기를 돌려 봐도 되지만 통과할 의무는 없다.
다만 6번(한 용어 한 뜻)은 기록에도 지킨다. 용어가 갈라지면 레슨이 흔들린다.

`valley/` 와 `llmops/` 의 기존 레슨은 이 규칙보다 먼저 쓰였다.
따로 요청받기 전까지 소급해서 고치지 않는다.

워크스페이스를 어떻게 굴리는지는 각 워크스페이스의 `NOTES.md` 에 있다.
저장소 구조와 빌드 방법은 [README.md](./README.md) 에 있다.
