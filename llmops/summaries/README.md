# Summaries

레슨을 읽은 뒤 **내 언어로 다시 쓴 글**이 여기 쌓인다. 파일 번호는 레슨 번호와 맞춘다
(`0002-*.md` ↔ `lessons/0002-*.html`).

- 형식과 채점 규칙: [/CLAUDE.md](../../CLAUDE.md)
- 이 글은 선생이 쓰지 않는다. 내가 쓴 문장이 아니면 이 파일은 의미가 없다.
- 사이트에 배포되지 않는다 (빌더 allow-list 밖).
- 통과한 요약은 나중에 블로그·리포트 초고로 그대로 재활용한다.
